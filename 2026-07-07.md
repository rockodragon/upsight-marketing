Worktrees already solve your DB problem — you just need to actually use them. I checked directly: from inside an existing worktree (.worktrees/feat-facet-foundation), bd where/bd info resolve to the exact same database path and issue count (1349) as your main checkout. Worktrees of the same repo auto-share one live beads database via "git common directory discovery" — there's no export/merge/reconcile step at all, because there's only one DB being read and written. To get a local worktree session, just tell me "work in a worktree" (or ask directly) — I'll use my EnterWorktree tool, which creates it under .claude/worktrees/, already gitignored, already wired to the shared DB. Nothing extra to configure.

The actual risk is cloud sessions, not worktrees — and I found two real gaps. A cloud-hosted session runs on a totally separate clone with its own independent beads database, seeded only from whatever .beads/issues.jsonl says at clone time. It really does just mutate its own DB and hand you back a jsonl diff to merge in — that part matches what you've observed. The safety net meant to make that merge-back reliable had two holes on this machine: (1) .gitattributes declares a conflict-aware custom merge driver for issues.jsonl, but it was never actually registered in git config, so any real conflict would've silently fallen back to plain text merging instead of ID-aware reconciliation; (2) the git hooks that resync your local DB after a pull/merge/checkout were sitting as unused template files — never installed into your live .git/hooks/. I fixed #2 just now (bd hooks install --chain, verified your existing lefthook pre-commit hook is untouched). I could not find a bd command to register the merge driver from #1 — I didn't want to hand-write git config for a mechanism I couldn't verify actually works, so that one's still open. Practically: your current DB is internally clean right now (1349 issues, zero duplicate IDs, matches the jsonl file exactly) — but I can't retroactively prove nothing was ever silently dropped in a past merge before hooks were installed.

The .worktrees/ directory you're seeing: confirmed — .gitignore only covered .claude/worktrees/, not the top-level .worktrees/ path where Codex (and one feat/facet-foundation worktree) checked out. It showed as untracked only because nobody had run git add yet — one git add -A away from committing two full repo copies by accident. Fixed, added .worktrees/ to .gitignore (uncommitted, sitting with your other pending changes). Also: both of those worktrees look stale — feat-facet-foundation has 6 uncommitted modified files and is 11 commits behind staging; the Codex one is 17 commits behind. Didn't touch either since one has real uncommitted work — worth a look before cleanup.


I had a conversation with a couple guys who I respect giving them a demo of Upsight and this is their feedback. They steered me towards a couple things focusing on sales teams who had really high performance sales teams that had people filling out forms. Could I be that better form for the intake and optimization?
# Phillip Powell & Jonathan

Deliver quote for my help to launch
1. roadmap for v1 + vnext
2. user story flows
3. Branding + Design Standards
4. Experience
5. Screen designs

$125/hr
DeepLight subcontract

## 7-13 AJ Keys to the City

Work to do:
ship mobile for ios/android
understand and make a call on existing QR process or adopting Rick's inverted QR code mode

Roadmap - v1
- 2 main feature areas of app
	- deals
		- collection of items: desc & disclaimers
		- can be scheduled, restricted (eg between certain hours, 1 per person)
		- can be for subs, or non-subs (everyone)
		- Finding deals
			- by business, diving in
			- list of deals
			- near me (by distance)
			- Semantic searching (eg coffee returns bag of beans)
		- Redeeming deals
			- assoc or owner has authority to grant deal & create voucher (redeemed instance of a deal). owner responsible for money. K2C not collecting financial details of transaction .
			- Proposed "Inverted model" QR Code - removes friction
			- POS integration - heavy LOE and business install friction
				- varies by system. clover seems like clear deploy path. unknown are Toast, square etc.
		- Reporting - None yet for members or businesses
	- events 
		- based on Luma model. 
			- draft status -> active
			- everyone or sub only
			- capacity
			- unlisted or public
			- locations
			- only admins can create events in v1			
			- non-goals
				- no money charging for events 
		- Finding
			- Distance from me
			- upcoming, past
		- Attending - Non-goal
			- how important are check-ins?
	- Infra & Ops
		- TODO deploy in capacitor
		- posthog error tracking - good
	- Timelines
		- TBD
	- Financial
- no money passed to us for interactions
- commerce piece for subscriptions from people. Pricing presumed $10/mo
- tiers for businesses? or sponsor level? 0 to start, x at 3 months, y at 6 months?
- payments via stripe
- what does market research say for similar businesses
- portion of revenue from events? financial model esp as it relates to app ecom features
- currently businesses dont create events. only admins. why is that? seems like a gating factor? but in early stage may be more like a concierge service. will be turned on later?
- admins can create events now?

V2 roadmap
...

## Todo: re User Flows
Audit flows and review brand brief and design

Output
Roadmap + Timelines + Risks + Mitigations

use upsight?


## Zaid - Feat/support transcript import
make sure that gets converted into a format that supports evidence clips for text only
downstream Reels func should be able to select text from a evidenceclip


Rerun analysis doesnt seem to fire off

https://github.com/epic-hq/UpSight/blob/claude/reels-nccc-pipeline-audit-y0v4f8/docs/20-features-prds/features/conversation-lenses/segment-scoped-lens-summaries.md


