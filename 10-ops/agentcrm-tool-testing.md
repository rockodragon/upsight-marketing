# AgentCRM tool testing rubric — moved

This rubric now lives in the **code repo** so agents working in `Insights/` auto-discover it (via `Insights/CLAUDE.md`):

→ **`Insights/docs/30-howtos/mastra-tools/agentcrm-testing-rubric.md`**

The vault was the wrong home — it's a code-domain testing checklist, not a business-narrative artifact. Keeping a one-line pointer here so vault readers can find it without going hunting.

The dogfooding-log (`10-ops/dogfooding-log.md`) stays in the vault on purpose — it's intentionally cross-domain (sales artifact + product backlog).
