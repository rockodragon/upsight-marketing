# Never Start at Zero

VSL — compounding profiles that never start cold. Ordinary forms die in a spreadsheet; UpSight remembers.

| | |
|--|--|
| Script / creative | `01-script/` |
| Remotion | `05-remotion/` |
| Master | 1920×1080 · ~0:51 · 30fps |
| Render | `05-remotion/out/NeverStartAtZero.mp4` |

**One idea:** You ask once. Every interaction compounds. You never start at zero.

```bash
cd 05-remotion && npm install && npm run studio
cd 05-remotion && npm run render   # → out/NeverStartAtZero.mp4
```

v1 is illustrative Remotion (no live VO yet). Scrub composition `NeverStartAtZero`.
