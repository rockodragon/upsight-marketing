# Never Start at Zero — Remotion

Composition `NeverStartAtZero` · 1920×1080 · 51s · 30fps · marketing dark.

```bash
npm install
npm run studio
npm run render    # → out/NeverStartAtZero.mp4
npm run still     # preview frame at the promise beat
```

Copy lives in `../01-script/hero.script.json`. Change captions or durations there, then re-render.

v1 uses illustrative UI (spreadsheet row, living profile, demo beats). Swap in `04-assets/ui/` stills when captures land.
