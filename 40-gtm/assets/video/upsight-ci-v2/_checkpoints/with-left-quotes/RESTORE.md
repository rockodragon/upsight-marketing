# Checkpoint: with-left-quotes

Saved before the media-first (no quote) left feed experiment.

## Restore

```bash
cp _checkpoints/with-left-quotes/SnippetFeed.tsx 05-remotion/src/components/
cp _checkpoints/with-left-quotes/hero.script.json 01-script/
cp _checkpoints/with-left-quotes/CiV2.tsx 05-remotion/src/compositions/
```

Then point the Zoom card file back to `video/meeting-panel.mp4` if needed.
